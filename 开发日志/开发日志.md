# 开发日志

## 2026-08-20

### 已完成
- Streamlit 应用骨架:浅黄主题 + 开屏页 + 左侧导航
- 核心流程:导入表格 → 日期筛选 → 分组 → 聚合 → 导出汇总表
- 透视表(行×列交叉)+ 下钻看明细
- 历史保存:自动存表 + 存分析结果 + 复用设置(SQLite + parquet)
- 三个测试脚本(test_core / test_ui / test_history),均通过
- 修复:日期筛选「漏掉当天数据」的边界 bug
- 建立开发规范:标准文件 + 本日志 + CLAUDE.md
- **按列值筛选(单条件)**:「选列 → 选值 → 筛选」+「清除筛选」按钮,默认不筛选
- 新增 test_filter.py(按列值筛选 UI 测试),核心场景验证:用户「张同学」→ 4 个主播 → 共 4379 元
- **视觉精修(温暖米黄)**:白卡片圆角阴影、hero 渐变标题栏、三步引导卡片、按钮/控件样式
- **文案专业化**:统一「导入 / 筛选 / 分组汇总 / 导出 / 存档」措辞,聚合→汇总、上传→导入、保存→存档
- **附加统计列(可选)**:记录笔数、最早日期、最晚日期,可勾选追加到汇总表(基于自动识别的日期列)
- **字体**:仿宋优先 + 宋体兜底(config `font=serif` + CSS 字体栈),法律文书风格
- **侧边栏 logo**:金色天平徽章(SVG)+ 标题
- **历史记录增强**:时间改中文显示(年月日)、文件可置顶(★ 排前)、文件备注(备忘录,可编辑)
- **UI 细节优化**:置顶改图钉图标、时间后加「更新」、侧栏 logo 贴顶、备忘录缩小、开屏三步渐入动画、「进入工作台」按钮放大突出
- **UI 再优化**:三步去方框改箭头(金色圆数字 + →)、按钮进一步加大、侧栏 logo 放大、历史页文件 tab 改「存档的文件」
- **UI 第三轮**:logo 从侧栏移到开屏页(换成黄色圆角方块+柱状图 SVG)、三步改圆形数字+→+淡入、全站字体改现代无衬线(仿宋→美团风格 PingFang/MiSans 栈)
- **品牌定制**:App 改名「法务部最会做表的那个同事」;开屏导言「统计而已,不必动怒。」;logo 换成熊猫头(老花镜 + 头顶问号谜团 + 手举放大镜,镜片内是柱状图)
- **开屏极简化**:删除三步走引导 + 描述介绍文案,只留 logo/名字/导言/大按钮,「进入工作台」按钮再次放大(1.7rem / 加粗 / 更大内边距与阴影)
- **侧栏精简**:去掉侧栏顶部 App 名字标题,只保留「导航」radio
- **上云部署(网页版)**:生成 requirements.txt + .gitignore、隐私文案改「演示环境·数据临时存储」、git 提交并推送 GitHub(icyuuuu1/legal-data-analysis)、部署到 Streamlit Cloud;修复 pyarrow 21 在 Python 3.14 无预编译包导致的构建失败(放宽版本约束)
- **桌面 App 图标**:生成熊猫 icns、打包成 .app 放桌面(双击打开网页版)、设置开机自动打开(LaunchAgent)
- **开屏使用背景说明**:新增可折叠「这个工具是做什么的?」,写明使用背景 + 四大适用场景(直播打赏退款 / 主播结算 / 电商退款 / 风险账号处置)

### 待办
- [ ] 多条件筛选:加「+ 添加条件」按钮,多条「列=值」AND 关系,每条可单独删除
- [ ] (可选)列值筛选也纳入「保存分析 / 复用设置」,现在保存的设置里暂不含筛选条件
- [ ] (可选)附加统计列也纳入「存档 / 复用设置」(存档结果正确,但复用时不自动勾选附加列)
